<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="css/skeleton.css">
    <link rel="stylesheet" href="css/style.css">
  </head>
  <body>
    <div class="container">
      <div class="row">

        <div class="twelve columns box">
          <h3>Bill with settings</h3>
          <div class="">
            Select the call or sms radio button. Update the appropriate total and the overall total when the ADD button in pressed.
            <i>Calls costs should now be configurable using the settings section.</i>
          </div>
          <div class="">
            <i>If the total cost exceed the warning level show the total cost in orange, if it exceeds the critical level show it in red and prevent any new costs from being added. The  warning and critical level should both be configurable now.</i>
          </div>

          <hr>

          <div class="row">

            <div class="six columns">
              <h5>Bill type</h5>
                <form action="/action" method="post">
                
                  <label class="">
                    <input type="radio" name="actionType" value="call" class="billItemTypeWithSettings">
                    <span class="label-body">Call</span>
                  </label>

                  <label class="">
                    <input type="radio" name="actionType" value="sms" class="billItemTypeWithSettings">
                    <span class="label-body" >SMS</span>
                  </label>
                  
                  {{#if criticalLevel}}
                    <button disabled class="button-primary" type="submit" name="button">Add</button>
                  {{else}}
                    <button class="button-primary" type="submit" name="button">Add</button>
                  {{/if}}

                  {{#if resetSettings}}
                  <button class="button-primary" type="submit" name="button">Reset Totals</button>
                  {{/if}}

  
                </form>


              <h5>Totals</h5>
                  <table>
                    <tr>
                      <td>
                        <a href="/actions/call">Call total</a>
                      </td>
                      <td>R<span class="callTotalSettings">{{totals.callTotal}}</span> </td>
                    </tr>
                    <tr>
                      <td>
                        <a href="/actions/sms">SMS total</a>
                      </td>
                      <td>R<span class="smsTotalSettings">{{totals.smsTotal}}</span> </td>
                    </tr>
                    <tr>
                      <td>
                        <a href="/actions">Total</a>
                      </td>

                      {{!-- {{#if criticalLevel}} --}}


                        <td> R<span class="{{classNames}}" class="totalSettings">{{totals.grandTotal}}</span> </td>


                      {{!-- {{else if warningLevel}} --}}
                        {{!-- <td> R<span class="warning" class="totalSettings">{{totals.grandTotal}}</span> </td> --}}
                      {{!-- {{/if}} --}}
                    </tr>
                  </table>

            </div>
            <div class="six columns">
              <h5>Settings</h5>
              
              <form action="/settings" method="post">


                {{!-- step=".01" attribute helps to be able to send decimal numbers --}}
              
                <label for="exampleEmailInput">Call cost</label>
                <input class="u-full-width callCostSetting" type="number" name="callCost" step=".01" placeholder="eg. 2.55" value="{{settings.callCost}} {{resetSettings.callCost}}">
                <label for="exampleEmailInput">SMS cost</label>
                <input class="u-full-width smsCostSetting" type="number" name="smsCost" step=".01" placeholder="eg. 0.65"  value="{{settings.smsCost}} {{resetSettings.smsCost}}">
                <label for="">Warning level</label>
                <input class="u-full-width warningLevelSetting " type="number" name="warningLevel" step=".01" placeholder="eg. 30.00" class="warningLevel" value="{{settings.warningLevel}} {{resetSettings.warningLevel}}">
                <label for="">Critical level</label>
                <input class="u-full-width criticalLevelSetting" type="number" name="criticalLevel" step=".01" placeholder="eg. 65.00" value="{{settings.criticalLevel}} {{resetSettings.criticalLevel}}">
                
                <button class="button-primary updateSettings" type="submit" name="button">Update settings</button>

                <button class="button-primary updateSettings" type="submit" name="button">Reset settings</button>


              </form>

            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
